export const formatBitcoinBalance = (balance: number) => {
  return balance.toFixed(8);
};

export const formatEthereumBalance = (balance: number) => {
  return balance.toFixed(18);
};

export const formatLusdBalance = (balance: number) => {
  return balance.toFixed(18);
};
